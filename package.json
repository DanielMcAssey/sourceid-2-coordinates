{
  "name": "sourceid-2-coordinates",
  "version": "1.0.0",
  "description": "A sourceId2Coordinates native module",
  "author": {
    "name": "Daniel McAssey",
    "email": "dan@glokon.me"
  },
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "gypfile": true,
  "scripts": {
    "build": "npm run build-native && npm run tsc",
    "tsc": "tsc",
    "build-native": "node-gyp rebuild",
    "install": "prebuild-install || electron-rebuild rebuild",
    "install-debug": "prebuild-install || node-gyp rebuild --debug",
    "prebuild": "prebuild --all"
  },
  "dependencies": {
    "nan": "^2.14.0",
    "prebuild-install": "^5.3.3",
    "electron-rebuild": "^1.11.0"
  },
  "devDependencies": {
    "@types/node": "12.11.1",
    "typescript": "^3.7.4"
  }
}
